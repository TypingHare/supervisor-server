package supervisor.common.constant

/**
 * Constants for HTTP request header names.
 * These keys are used to access standard HTTP headers like Authorization
 * when processing incoming requests.
 */
object HeadersKey {
    const val AUTHORIZATION = "Authorization"
}